apiVersion: v1
kind: Service
metadata:
  name: ppe-service
spec:
  type: NodePort
  selector:
    app: ppe-app
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007